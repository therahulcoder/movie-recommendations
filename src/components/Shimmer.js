import React from 'react';
import { shimmerStyles as styles } from '../styles/styles';

const Shimmer = () => (
  <div style={styles.shimmerWrapper}>
    <div style={styles.shimmer}></div>
  </div>
);

export default Shimmer;
